# Nothing in this directory is generated: it is materialized read-only from the
# Nix store, and Neovim writes its state to ~/.config/nvim instead. Keep only
# editor/OS noise out of git.
*.swp
*.swo
.DS_Store
