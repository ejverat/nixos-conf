set -g default-terminal "xterm-256color"
set -g extended-keys on

unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

bind r source-file ~/.dotfiles/config/tmux/tmux.conf \; display "Reloaded config"
set -g base-index 1

set-window-option -g mode-keys vi
set-window-option -g xterm-keys on
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# vim-like pane switching
bind -r ^ last-window
bind -r k select-pane -U
bind -r j select-pane -D
bind -r h select-pane -L
bind -r l select-pane -R

#custom binds
bind-key -r i run-shell "tmux neww ~/.dotfiles/utilities/cht.sh"

#visual config
#set -g status-style 'bg=#333333 fg=#5eacd3'

# image.nvim/molten: visual-activity off + focus-events on para que las
# imágenes se rendericen y se oculten bien al cambiar de ventana/pane
set -g visual-activity off
set -g focus-events on
